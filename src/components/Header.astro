---
import Burger from "./Burger.astro";
import Navigation from "./Navigation.astro";
import Logo from "./Logo.jsx";

// const fonts = [
//    {
//       fontName: "seascape"
//    },
//    {
//       fontName: "jamesarthur"
//    }
// ]

const { shadow } = Astro.props;
---

<!-- {
   fonts.map(({fontName}) => {
      return (
      <header class="p-5 md:px-6 mx-auto mb-5 max-w-7xl relative shadow-md md:shadow-none md:flex md:justify-between md:items-center">
   <div class="flex justify-between mb-4 md:mb-0">
      <Logo fontName={fontName} />
   
      <div class="flex flex-col">
         <Burger />
      </div>
   </div>
   <Navigation />
</header>
      )
   })
} -->

<header class={
   `absolute top-0 text-slate-950 w-full z-40 before:backdrop-blur-lg dark:text-slate-100 md:sticky md:overflow-y-auto md:min-w-[18rem] md:w-1/3
   ${shadow ? "bg-white/50 shadow-xl dark:bg-stone-950/50 dark:shadow-slate-950/20" : "bg-gray-50/90 dark:bg-gray-900/60"}`
}>
   <!-- <div class="md:sticky md:top-0 md:overflow-y-auto md:pb-24"> -->
      <div class="px-4 pt-7 pb-7 mx-auto max-w-screen-2xl md:border-0 md:overscroll-contain md:max-h-screen md:px-6 md:flex md:flex-col md:gap-10 md:justify-between">
         <div class="top-0 flex justify-between gap-2 md:backdrop-blur-md md:bg-gray-50/40 md:sticky md:z-50 md:pl-4 md:py-5 dark:md:bg-gray-900/40">
            <Logo />
         
            <Burger />   
         </div>
         <Navigation />
      </div>
   <!-- </div> -->
</header>

<style>
   header::before {
      @apply absolute inset-0 -z-10;
   }

   .patties::before {
      @apply top-5;
   }
   
   .patties::after {
      @apply bottom-4 right-1.5;
   }

   .patties::before,
   .patties::after {
      content: '';
      @apply absolute h-[2px] w-10 bg-gray-900;
   }
</style>